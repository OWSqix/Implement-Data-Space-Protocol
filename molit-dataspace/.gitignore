# Browser authentication and raw automation artifacts
.auth/
cdp-profile/
playwright-report/
test-results/
evidence/raw/
**/*.har
**/trace.zip
**/storage-state*.json
**/storageState*.json
**/storage.json
**/cookies*.json
**/session*.json

# Local secrets
.env
.env.*
!.env.example

# Test-only TLS material, generated at test time by
# tests/fixtures/identity-tls/generate.mjs and never committed. These keys exist
# only for the duration of a test run and are registered in no trust store.
tests/fixtures/identity-tls/*.crt
tests/fixtures/identity-tls/*.key

# Test-only namespace TLS material, generated at test time by
# tests/namespace/fixtures/generate.mjs and never committed. This key exists
# only for the duration of a test run and is registered in no trust store.
tests/namespace/fixtures/*.crt
tests/namespace/fixtures/*.key
tests/namespace/fixtures/*.p12
tests/namespace/fixtures/*.pfx

# Local Bridge state and generated reports
.local/
test-results/
node_modules/
