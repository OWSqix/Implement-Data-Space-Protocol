# Third-party notices

The smoke-only Data Plane classes under `runtime/smoke-data-plane` adapt code
from Eclipse EDC Samples. The source revision and copied-file digests are fixed
in `upstream-lock.json`. Those classes remain under Apache License 2.0.

The checked-in Gradle wrapper files are distributed under Apache License 2.0.
The wrapper version and digests are also fixed in `upstream-lock.json`.

The license text is in [`licenses/Apache-2.0.txt`](licenses/Apache-2.0.txt).
