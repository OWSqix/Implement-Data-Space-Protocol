server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    default_type application/json;
    location / {
        limit_except GET { deny all; }
        try_files $uri =404;
    }
}
